<div class="container mx-auto px-4 py-8 max-w-lg">
  <h1 class="text-3xl font-bold mb-6">New Post</h1>

  <%= render "form", post: @post %>

  <%= link_to t("shared.back"), posts_path, class: "text-blue-600 hover:underline mt-4 inline-block" %>
</div>
